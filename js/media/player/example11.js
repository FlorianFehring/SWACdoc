window['player_example11_options'] = {
    allowDel: true,
    customAfterRemoveSet: function (requestor) {
        // Code to directly execute after playlist entry was removed
        // e.g. remove connection from database
    }
};
